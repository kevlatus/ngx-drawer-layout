import { DrawerToggleDirective } from './drawer-toggle.directive';

describe('DrawerToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerToggleDirective();
    expect(directive).toBeTruthy();
  });
});
