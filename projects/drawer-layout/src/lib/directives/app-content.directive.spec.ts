import {DrawerAppContentDirective} from './drawer-app-content.directive';

describe('DrawerAppContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerAppContentDirective();
    expect(directive).toBeTruthy();
  });
});
