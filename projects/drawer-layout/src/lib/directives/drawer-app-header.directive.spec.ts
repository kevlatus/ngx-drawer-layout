import {DrawerAppHeaderDirective} from './drawer-app-header.directive';

describe('DrawerAppHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerAppHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
