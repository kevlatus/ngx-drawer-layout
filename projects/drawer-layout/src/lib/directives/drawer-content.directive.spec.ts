import {DrawerContentDirective} from './drawer-content.directive';

describe('DrawerContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerContentDirective();
    expect(directive).toBeTruthy();
  });
});
