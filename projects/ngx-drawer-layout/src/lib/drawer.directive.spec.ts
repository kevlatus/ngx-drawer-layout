import { DrawerDirective } from './drawer.directive';

describe('DrawerDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerDirective();
    expect(directive).toBeTruthy();
  });
});
